version: "3.9"

services:
  backend:
    build: ./movie-app-backend-master
    container_name: movie-backend
    image: abhishekjadhav1996/movie-backend:latest   # ✅ added
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: "${MONGO_URI}"

  gateway:
    build: ./api-gateway
    container_name: movie-gateway
    image: abhishekjadhav1996/movie-gateway:latest   # ✅ added
    ports:
      - "8000:8000"
    depends_on:
      - backend
    environment:
      BACKEND_URL: "http://backend:5000"

  frontend:
    build: ./movie-app-frontend-master
    container_name: movie-frontend
    image: abhishekjadhav1996/movie-frontend:latest  # ✅ added
    ports:
      - "3000:3000"
    depends_on:
      - gateway
    environment:
      API_GATEWAY_URL: "http://gateway:8000"




# version: "3.9"

# services:
#   backend:
#     build: ./movie-app-backend-master
#     container_name: movie-backend
#     ports:
#       - "5000:5000"
#     environment:
#       DB_HOST: "${DB_HOST}"
#       DB_USER: "${DB_USER}"
#       DB_PASSWORD: "${DB_PASSWORD}"

#   gateway:
#     build: ./api-gateway
#     container_name: movie-gateway
#     ports:
#       - "8000:8000"
#     depends_on:
#       - backend
#     environment:
#       BACKEND_URL: "http://backend:5000"

#   frontend:
#     build: ./movie-app-frontend-master
#     container_name: movie-frontend
#     ports:
#       - "3000:3000"
#     depends_on:
#       - gateway
#     environment:
#       API_GATEWAY_URL: "http://gateway:8000"
